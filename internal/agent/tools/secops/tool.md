Tool registry and shared interface for all SecOps tools.

This file defines `ToolType`, `SecOpsTool`, and `SecOpsToolRegistry`. It is used
internally to register and look up tools. It is not a user-facing tool and has
no direct CLI parameters.
