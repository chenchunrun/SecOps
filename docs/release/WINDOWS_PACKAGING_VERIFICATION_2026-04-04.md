# Windows Packaging Verification Record (2026-04-04)

> Local execution record for Windows release-package verification.
> Fill this out in the actual packaging/release environment.

## Scope

- Windows package contents
- Binary naming and compatibility alias
- Install/uninstall script behavior
- Basic launch verification

Related references:

- [`POST_RELEASE_CHECKLIST.md`](/Users/newmba/SecOpsCode/crush-main/docs/release/POST_RELEASE_CHECKLIST.md)
- [`INSTALL_ONECLICK_zh-CN.md`](/Users/newmba/SecOpsCode/crush-main/INSTALL_ONECLICK_zh-CN.md)
- [`scripts/package_cross_platform.sh`](/Users/newmba/SecOpsCode/crush-main/scripts/package_cross_platform.sh)

## Environment

- Date: `2026-04-04`
- Operator: Codex precheck + release operator
- Windows version:
- Packaging host:
- Verification host:
- Version/package under test:

## Automated Precheck

Confirmed before Windows-host verification:

- Packaging script target matrix includes `windows/amd64` and `windows/arm64` in
  [`scripts/package_cross_platform.sh`](/Users/newmba/SecOpsCode/crush-main/scripts/package_cross_platform.sh).
- Windows package naming contract in script:
  - primary binary: `secops-agent.exe`
  - compatibility alias: `crush.exe`
  - install script: `install.ps1`
  - uninstall script: `uninstall.ps1`
  - default install path: `%LOCALAPPDATA%\Programs\SecOps`
- Windows install documentation in
  [`INSTALL_ONECLICK_zh-CN.md`](/Users/newmba/SecOpsCode/crush-main/INSTALL_ONECLICK_zh-CN.md)
  matches the same naming/path contract.

Known precheck outcome from current non-Windows environment:

- Local binary smoke check already passed for the mainline build path.
- A full Windows packaging smoke check was not completed in this environment;
  earlier cross-build/package attempts were limited by local execution/runtime
  constraints rather than by a confirmed script logic failure.

## Expected Package State

- Package archive exists for the tested target.
- Package contains:
  - `secops-agent.exe`
  - `crush.exe`
  - `install.ps1`
  - `uninstall.ps1`
  - `INSTALL.md`
  - checksum files for both executables and the archive
- Default install target:
  - `%LOCALAPPDATA%\Programs\SecOps`

## Checklist

### 1. Package Contents

- [ ] Archive filename matches expected version and target.
- [ ] `secops-agent.exe` exists in package root.
- [ ] `crush.exe` exists in package root.
- [ ] `install.ps1` exists.
- [ ] `uninstall.ps1` exists.
- [ ] `INSTALL.md` exists.
- [ ] Checksums exist for archive and both executables.
- Notes:
  - Expected contents are generated by
    [`scripts/package_cross_platform.sh`](/Users/newmba/SecOpsCode/crush-main/scripts/package_cross_platform.sh).

### 2. Binary Naming Contract

- [ ] `secops-agent.exe` is treated as the primary executable.
- [ ] `crush.exe` is present only as a compatibility alias.
- [ ] Documentation in package matches this naming contract.
- Notes:
  - Script contract and install doc contract already match before manual
    verification.

### 3. Installation Flow

- [ ] `install.ps1` runs without unexpected prompts or path errors.
- [ ] Default install path resolves to `%LOCALAPPDATA%\Programs\SecOps`.
- [ ] Both `secops-agent.exe` and `crush.exe` are copied to target directory.
- [ ] PATH update behavior is correct for the current user.
- Notes:
  - Script default target is `%LOCALAPPDATA%\Programs\SecOps`.

### 4. Launch Verification

- [ ] `secops-agent --version` succeeds after install.
- [ ] `crush --version` succeeds as compatibility alias after install.
- [ ] Primary executable can launch the application.
- Notes:

### 5. Uninstall Flow

- [ ] `uninstall.ps1` removes `secops-agent.exe`.
- [ ] `uninstall.ps1` removes `crush.exe`.
- [ ] No unexpected leftovers remain in target directory.
- Notes:

## Result Summary

- PASS/FAIL:
- Blocking issues:
- Non-blocking issues:
- Follow-up actions:

## Reference Pointers

- Packaging script:
  [`scripts/package_cross_platform.sh`](/Users/newmba/SecOpsCode/crush-main/scripts/package_cross_platform.sh)
- Windows install guide:
  [`INSTALL_ONECLICK_zh-CN.md`](/Users/newmba/SecOpsCode/crush-main/INSTALL_ONECLICK_zh-CN.md)
- Overall closeout checklist:
  [`POST_RELEASE_CHECKLIST.md`](/Users/newmba/SecOpsCode/crush-main/docs/release/POST_RELEASE_CHECKLIST.md)
